# Appointment Board API

FastAPI service for the Appointment Board. It uses SQLAlchemy with PostgreSQL and exposes OpenAPI documentation at `/docs`.

Run from this directory with `uvicorn app.main:app --reload` after installing the root requirements.